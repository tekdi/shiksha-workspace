// src/theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // Customize your theme color
    },
    secondary: {
      main: "#dc004e",
    },
  },
});

export default theme;
